{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress": "npx cypress open",
    "cy:open": "npx cypress open",
    "cy:run": "npx cypress run"
  },
  "devDependencies": {
    "@bahmutov/cy-grep": "^1.7.2",
    "cypress": "^12.7.0"
  }
}
