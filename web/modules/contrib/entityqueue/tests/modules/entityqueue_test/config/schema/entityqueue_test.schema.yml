# Schema for the entity queue 'test' handler configuration.
entityqueue_handler_configuration.test:
  type: entityqueue_handler_configuration
  label: 'Test handler configuration'
  mapping:
    shape:
      type: string
      label: 'A test property.'
