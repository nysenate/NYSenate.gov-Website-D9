<?php

/**
 * @file
 * Hooks specific to the Font Awesome module.
 */

/**
 * @addtogroup hooks
 * @{
 */

/**
 * Alter the Font Awesome categories metadata.
 *
 * @param array &$metadata
 *   An array representing the categories metadata.
 */
function hook_fontawesome_metadata_categories_alter(array &$metadata): void {
  // Alter the metadata as needed.
}

/**
 * Alter the Font Awesome icon metadata.
 *
 * @param array &$metadata
 *   An array representing the icon metadata.
 */
function hook_fontawesome_metadata_alter(array &$metadata): void {
  // Alter the metadata as needed.
}

/**
 * @} End of "addtogroup hooks".
 */
