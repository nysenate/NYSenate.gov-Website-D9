/**
 * @file
 *  Handler for search legislation.
 */
 'use strict';
 (function ($, Drupal, drupalSettings) {
     // Custom return handler for AJAX legislation submission.

 })(jQuery, Drupal, drupalSettings);
