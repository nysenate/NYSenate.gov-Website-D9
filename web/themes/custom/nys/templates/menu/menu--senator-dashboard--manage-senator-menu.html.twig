{% set columns = items|map((item, key) => include('nys:admin-list', {
  machine_name: key,
  items: item.below,
  title: item.title,
}, with_context = false)) %}

{{ include('nys:columns', {
  columns: columns,
  style: 'ruled',
}, with_context = false) }}
