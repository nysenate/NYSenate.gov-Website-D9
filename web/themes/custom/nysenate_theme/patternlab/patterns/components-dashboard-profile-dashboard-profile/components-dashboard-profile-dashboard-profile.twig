{{ attach_library('nysenate_theme/dashboard-profile') }}

<div class="dashboard-profile">
  <div class="c-block c-container c-container--dash-user-card">
    <div class="c-user--header">
      <h2 class="c-container--title">
        {{ dashboard_profile.first_name }}
        {{ dashboard_profile.last_name }}
      </h2>
    </div>
    <div class="dash-user-info">
      <div class="dash-user-img">
        {{ dashboard_profile.image }}
      </div>
      <div class="c-container--body">
        <div class="c-dash-user-addr">
          <div class="location vcard" itemscope="" itemtype="http://schema.org/PostalAddress">
            <div class="adr">
              <div class="street-address">
                <span itemprop="streetAddress">{{ dashboard_profile.street_address }}</span>
              </div>
              <span class="locality" itemprop="addressLocality">{{ dashboard_profile.city }},</span>
              <span class="region" itemprop="addressRegion">{{ dashboard_profile.state }}</span>
              <span class="postal-code" itemprop="postalCode">{{ dashboard_profile.zip }}</span>
            </div>
          </div>
        </div>
        <div class="c-container--email vcard">
          <a href="mailto:{{ dashboard_profile.email }}">{{ dashboard_profile.email }}</a>
        </div>
      </div>
      <div class="c-dash-user-action-links">
        <a href="{{ dashboard_profile.edit_url }}" class="c-dash-edit-profile icon-before__petition">Edit</a>
      </div>
    </div>
  </div>
  <div class="c-block c-container c-container--dash-senator-card">
    <div class="c-user--header">
      <div class="c-container--title c-container--your-senator">
        My Senator
        <div class="c-container--body">
          <div class="c-dash-senator-action-links">
            <button class="c-dash-msg-senator icon-before__contact">
              Message
            </button>
          </div>
        </div>
      </div>
    </div>
    <div class="dash-senator-info">
      <div class="dash-senator-img">
        <a href="{{ dashboard_profile.senator.link }}">{{ dashboard_profile.senator.image }}</a>
      </div>
      <a href="{{ dashboard_profile.senator.link }}">
        <h2 class="c-container--title">
          {{ dashboard_profile.senator.name }}
        </h2>
        <span class="duties">{{ dashboard_profile.senator.duties }}</span>
        <p>
          {{ dashboard_profile.senator.committee }}
        </p>
        <h3 class="c-container--district">
          District
          {{ dashboard_profile.senator.district }}
        </h3>
      </a>
    </div>
  </div>

  <div class="msg-senator-modal">
    <div class="content">
      <span class="close">&times;</span>
      <label for="senator-message-subject">Subject:</label>
      <input type="text" id="senator-message-subject" name="subject" value="" size="60" maxlength="255" class="form-text"/>

      <label for="senator-message-body">Body:</label>
      <textarea type="text" id="senator-message-body" name="body" value="" size="60" class="form-text" rows="4"></textarea>
      <button class="button" id="senator-message-submit" name="submit-btn" value="Send message" type="submit">Send message</button>
    </div>
  </div>
</div>
