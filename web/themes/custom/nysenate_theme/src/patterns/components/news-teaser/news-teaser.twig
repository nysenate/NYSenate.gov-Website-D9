{{ attach_library('rain_theme/news-teaser') }}
{% import '@rain_theme/icons/_icons-macro.twig' as icons %}

{%
  set classes = [
    'news-teaser'
  ]
%}

{% set attributes = attributes ? attributes.addClass(classes) : ' class="' ~ classes|join(' ') ~ '"' %}

<div {{ attributes|raw }}>
  {% if image  %}
    <div class="news-teaser__image">
      {{ image }}
    </div>
  {% endif %}
  <div class="news-teaser__content">
    {%
      include '@rain_theme/heading/heading.twig' with {
        "heading": {
          "title": title,
          "heading_level": "2",
          "url": link,
          "modifier": "news-teaser__title",
        }
      } only
    %}
    <div class="news-teaser__date">
      {{ authored_date }}
    </div>
    {% if summary  %}
      <div class="news-teaser__summary">
        {{ summary|raw }}
      </div>
    {% endif %}
    {% if link %}
      <a class="news-teaser__link" href="{{ link }}"> Read more {{ icons.get('icon-chevron') }}</a>
    {% endif %}
  </div>
</div>
