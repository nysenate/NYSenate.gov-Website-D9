{#
/**
 * @file
 * Default theme implementation to display a block content.
 *
 * @see template_preprocess_block_content_template()
 *
 * @ingroup themeable
 */
#}
{%
  set classes = [
    'block-content',
    'block-content--type-' ~ bundle|clean_class,
    'block-content--' ~ id,
    'block-content--view-mode-' ~ view_mode|clean_class
  ]
%}

{% include "@nysenate_theme/hero/hero.twig" with {
  type: 'senator',
  is_senator_landing: is_senator_landing,
  is_logged: logged_in,
  senator_microsite_link: senator_microsite_link,
  image_hero: image_hero,
  senator_headshot: senator_headshot,
  default_image: "",
  is_active: is_active,
  senator_name: senator_name,
  inactive_senator_message: inactive_senator_message,
  current_duties: current_duties,
  senator_party: senator_party,
  district_number: district_number,
  socials: socials,
  user_id: user_id,
  message_senator_link: message_senator_link,
  your_dashboard_link: "/dashboard",
  incoming: incoming
} only %}
