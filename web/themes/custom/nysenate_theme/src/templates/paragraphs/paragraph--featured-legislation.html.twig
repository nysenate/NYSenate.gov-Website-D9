{#
/**
 * @file
 * Default theme implementation to display a paragraph.
 *
 * Available variables:
 * - paragraph: Full paragraph entity.
 *   Only method names starting with "get", "has", or "is" and a few common
 *   methods such as "id", "label", and "bundle" are available. For example:
 *   - paragraph.getCreatedTime() will return the paragraph creation timestamp.
 *   - paragraph.id(): The paragraph ID.
 *   - paragraph.bundle(): The type of the paragraph, for example, "image" or "text".
 *   - paragraph.getOwnerId(): The user ID of the paragraph author.
 *   See Drupal\paragraphs\Entity\Paragraph for a full list of public properties
 *   and methods for the paragraph object.
 * - content: All paragraph items. Use {{ content }} to print them all,
 *   or print a subset such as {{ content.field_example }}. Use
 *   {{ content|without('field_example') }} to temporarily suppress the printing
 *   of a given child element.
 * - attributes: HTML attributes for the containing element.
 *   The attributes.class element may contain one or more of the following
 *   classes:
 *   - paragraphs: The current template type (also known as a "theming hook").
 *   - paragraphs--type-[type]: The current paragraphs type. For example, if the paragraph is an
 *     "Image" it would result in "paragraphs--type--image". Note that the machine
 *     name will often be in a short form of the human readable label.
 *   - paragraphs--view-mode--[view_mode]: The View Mode of the paragraph; for example, a
 *     preview would result in: "paragraphs--view-mode--preview", and
 *     default: "paragraphs--view-mode--default".
 * - view_mode: View mode; for example, "preview" or "full".
 * - logged_in: Flag for authenticated user status. Will be true when the
 *   current user is a logged-in member.
 * - is_admin: Flag for admin user status. Will be true when the current user
 *   is an administrator.
 *
 * @see template_preprocess_paragraph()
 *
 * @ingroup themeable
 */
#}
{{ attach_library('nysenate_theme/featured-legislation') }}

<div class="block-content__content">
    {% include "@nysenate_theme/featured-legislation/featured-legislation.twig" with {
        bill: content.field_featured_bill,
        sponsor: 'Senator',
        senator_name: senator_name,
        senator_link: senator_link,
        quote: content.field_featured_quote.0['#context'].value,
        identifier: identifier ?? '',
        senator_image: senator_image,
        party: party,
        district: district_number
    } only %}
</div>
{#
<section class="c-legislation">
    {% if title %}
        <div class="c-container--header__top-border">
            <h3 class="c-container--title">{{ title }}</h3>
        </div>
    {% endif %}

    <div class="c-block c-block-legislation c-block-legislation-featured featured-legislation{% if identifier %} {{ identifier }} {% endif %}">
        <button class="js-leg-toggle c-block--btn c-block--btn-toggle">close</button>
        <div class="c-social">
            <ul class="c-social--list">
                <li>
                    <a target="_blank" href="https://www.facebook.com/" class="icon-replace__facebook">facebook</a>
                </li>
                <li>
                    <a target="_blank" href="http://twitter.com/" class="icon-replace__twitter">twitter</a>
                </li>
            </ul>
        </div>
        <!--embed featured bill view mode -->
        {{ content.field_featured_bill }}

        <div class="c-legislation--quote">
            <div class="c-quote--content">
                <h4 class="c-quote--title">Senator's position</h4>
                <p class="c-pullquote icon-before__quotes">
                    {{ content.field_featured_quote.0['#context'].value }}
                </p>
                <span class="c-pullquote--citation icon-before__minus u-quote--has-img">Senator<a href="{{ senator_link }}">{{ senator_name }}</a></span>
                <div class="c-legislation--sponsor">
                    <div class="c-sponsor--img">
                        <a href="{{ senator_link }}">{{ senator_image }}</a>
                    </div>
                    <div class="c-sponsor--info">
                        <p class="c-sponsor--name">
                            <a href="{{ senator_link }}">{{ senator_name }}</a>
                        </p>
                        {% if party %}
                            <p class="c-sponsor--party">{{ party }}</p>
                        {% endif %}
                        {% if district_number %}
                            <a class="c-sponsor--dist" href="{{ senator_link }}/district">{{ district_number }} District</a>
                        {% endif %}
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
#}
