{#
/**
 * @file
 * Theme override to display a taxonomy term.
 *
 * Available variables:
 * - url: URL of the current term.
 * - name: (optional) Name of the current term.
 * - content: Items for the content of the term (fields and description).
 *   Use 'content' to print them all, or print a subset such as
 *   'content.description'. Use the following code to exclude the
 *   printing of a given child element:
 *   @code
 *   {{ content|without('description') }}
 *   @endcode
 * - attributes: HTML attributes for the wrapper.
 * - page: Flag for the full page state.
 * - term: The taxonomy term entity, including:
 *   - id: The ID of the taxonomy term.
 *   - bundle: Machine name of the current vocabulary.
 * - view_mode: View mode, e.g. 'full', 'teaser', etc.
 *
 * @see template_preprocess_taxonomy_term()
 */
#}
{%
  set classes = [
    'taxonomy-term',
    'vocabulary-' ~ term.bundle|clean_class,
  ]
%}
{% if not active_senator %}
  {% set senator_copy = 'Your district’s state senate seat is currently vacant. For constituent services, please visit:' %}
{% elseif no_district %}
  {% set senator_copy = 'Your address does not match a NY State Senate District' %}
{% endif %}

<div class="c-block c-container c-container--dash-senator-card">
  <div class="c-user--header">
    {% if content.field_member_headshot %}
      {{ content.field_member_headshot }}
	  {% else %}
      <img src="{{ file_url('/themes/custom/nysenate_theme/src/assets/default-avatar.png') }}" alt="Default avatar"/>
	  {% endif %}
    <div class="dash-senator-info">
      {% if active_senator %}
        <div class="c-container--your-s">My Senator</div>
        <h2 class="c-container--title">{{ content.field_senator_name }}</h2>
        <span class="nys-senator--party">
          {{ party }}
        </span>

        <p>{{ content.field_current_duties }}</p>
        <h3 class="c-container--district">{{ 'District ' ~ district }}</h3>
	    {% else %}
        {% set inactive_class = 'inactive' %}
        <div class="c-container--your-s">{{ senator_copy }}<a href="/district/{{ district }}"><br>NY State District {{ district }} Page</a></div>
	    {% endif %}
    </div>
  </div>
</div>
